# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Config MCP locale (contiene chiavi API: mai nel repo)
.mcp.json

# Certificati di sviluppo generati con mkcert (contengono una CHIAVE PRIVATA).
# Sono locali alla macchina e si rigenerano con `npm run certs`: mai nel repo.
certs/

# Dump alimenti per `npm run import:foods` (grandi: restano locali, mai nel repo).
# Il dataset esercizi (data/exercises.json) invece è tracciato: non combacia con questi.
data/*.jsonl
data/*.jsonl.gz
data/*.gz
data/FoodData_Central_*.json
data/surveyDownload.json

# Script di esplorazione one-off dei dump (prefisso _): scratch locale, non nel repo
scripts/_*.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
